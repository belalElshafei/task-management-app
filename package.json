{
    "name": "task-management-app",
    "private": true,
    "scripts": {
        "backend:dev": "npm run dev --workspace=@task-management/backend",
        "backend:start": "npm run start --workspace=@task-management/backend",
        "frontend:dev": "npm run dev --workspace=@task-management/frontend",
        "frontend:build": "npm run build --workspace=@task-management/frontend",
        "backend:test": "npm test --workspace=@task-management/backend",
        "test": "npm run backend:test"
    },
    "workspaces": [
        "apps/*"
    ]
}
